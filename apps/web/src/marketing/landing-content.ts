export const landingCtas = [
  { label: "Start Private RFQ", href: "/trade" },
  { label: "Explore Proof", href: "/proof" },
] as const;

export const landingCopy = [
  "Private FXRP execution. Verifiable settlement.",
  "Keep seller and provider inputs encrypted through confidential computation.",
  "Publish only the public result needed for onchain settlement and verification.",
] as const;

export const lifecycleSteps = ["Create", "Quote", "Resolve", "Claim"] as const;
